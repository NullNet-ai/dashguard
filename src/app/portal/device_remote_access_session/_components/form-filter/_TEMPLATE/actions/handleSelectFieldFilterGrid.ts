const handleSelectFieldFilterGrid = async (): Promise<any> =>
  // data
  {
    alert("Select Field Filter Grid");
    return await Promise.resolve({});
  };

export default handleSelectFieldFilterGrid;
