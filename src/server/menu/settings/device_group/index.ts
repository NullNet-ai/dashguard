import { getGridLink } from '~/components/platform/Grid/utils/grid-get-link';

const menu = {
  title: 'Device Groups',
  url: getGridLink({ mainEntity: 'device_group' }),
  icon: 'RectangleGroupIcon',
};

export default menu;
