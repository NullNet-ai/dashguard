import { ISidebarMenu } from "~/components/platform/SideBar/type";

const menu = {
  title: "Dashboard",
  icon: "AcademicCapIcon",
  url: "/portal/dashboard",
  items: [],
} as ISidebarMenu;

export default menu;
